apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: trino-claim0
  name: trino-claim0
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
